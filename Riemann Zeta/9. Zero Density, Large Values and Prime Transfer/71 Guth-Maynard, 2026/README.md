# 71 Guth--Maynard, 2026

This node is the physical source root for the completed, frozen Guth--Maynard formalization. `RiemannZeta.lean` is the package root and `RiemannZeta/` contains all 303 project modules. The module names remain `RiemannZeta.*`; the project-root `lakefile.toml` points `srcDir` here so the numbered research layout does not alter the verified import graph.

See [the project README](../../README.md), [the source freeze](../../verification/SOURCE_FREEZE.md), and [the publication-readiness packet](../../Publication%20Readiness%20and%20Semantic%20Audit.md).
